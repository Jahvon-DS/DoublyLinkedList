package listListHomework;

public class LinkedList {

	static Node head = null; 
	
	class Node{
		int data;
		Node next,prev; 
		
		Node(int d){
			data = d; 
			next = prev = null; 
			
		}
	}
	// creating the doubly linked list 
	list.push(head, 2);
	lsit.push(head, 4);
	list.push(head, 8); 
	list.push(head, 10);
	
	System.out.println("Original Linked List");
	list.printList(head);
	
	void delete Node(Node head_ref, Node del){
		
		if 
	}
}
